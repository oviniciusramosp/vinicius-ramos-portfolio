/**
 * City page hover is handled inside bootTravelFilter (bindCardHover).
 * Kept as a no-op export so older imports don't break.
 */
export function bootTravelPlaceHover(): void {
  /* place hover wired in travel-filter.ts */
}
